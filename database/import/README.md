# USDA Import Folder

Put the unzipped USDA CSV files in this folder on your server when we get to the import step.

The first CSV we will import is:

`food.csv`

Do not upload the full USDA ZIP here until the database connection test passes.
